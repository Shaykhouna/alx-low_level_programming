#include "main.h"

/**
 * leet - encoder
 *
 * This function encodes a string into 1337
 * @: variable
 *
 * Return: Return the encided string
 */
char *leet(char *str)
{
	char i;

	if (i >= 'a' && i<= 'z')
	{
		while 
	}
}
