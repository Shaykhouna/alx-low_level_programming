#include <stdio.h>
#ifndef ABS_H
#define ABS_H

#define ABS(x) (((x) < 0) ? -(x) : (x))

/**
 * ABS - absolute
 * @x: parameter
 * ABS(x) - This function-like macro that computes the absolute value of a number
 */

#endif /* ABS_H */
