#include <stdio.h>
#ifndef ABS_H
#define ABS_H

#define ABS(x) (((x) < 0) ? -(x) : (x))

/**
 * main - absolute
 * This function-like macro that computes the absolute value of a number
 * @x: parameter
 * ABS(x)
 */

#endif /* ABS_H */
