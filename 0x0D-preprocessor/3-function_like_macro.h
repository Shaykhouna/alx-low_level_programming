#include <stdio.h>
#ifndef ABS_H
#define ABS_H

#define ABS(x) (((x) < 0) ? -(x) : (x))

/**
 * main - macro
 * function-like macro ABS(x) that computes the absolute value of a number
 * @x: arg
 * ABS(x)
 */

#endif /* ABS_H */
