#include <stdio.h>
#ifndef ABS
#define ABS(x) (((x) < 0) ? -(x) : (x))

/**
 * ABS - absolute
 * ABS(x) - This function-like macro that computes the absolute value of a number
 * @x: variable used
 */

#endif /* ABS */
