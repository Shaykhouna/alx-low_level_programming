#ifndef ABS
#define ABS(x) \
	do \
	{ \
		if (x < 0) \
			x = x * (-1); \
	} while (x != NULL)

/**
 * main - absolute
 *
 * This function-like macro ABS that computes the absolute value of a number
 *
 * Return: nada
 */

#endif /* ABS */
