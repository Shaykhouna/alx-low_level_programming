#include <stdio.h>
#ifndef ABS
#define ABS(x) (((x) < 0) ? -(x) : (x))

/**
 * ABS - absolute
 * This function-like macro ABS that computes the absolute value of a number
 * @x: variable used
 * ABS(x)
 */

#endif /* ABS */
