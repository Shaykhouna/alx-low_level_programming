#ifndef SUM

#define SUM(x, y) ((x) + (y))

/**
 * main - macro
 * function-like macro SUM(x, y) that computes the sum of the numbers
 * @x: first arg
 * @y: second arg
 * SUM(x, y)
 * Return: nada
 */

#endif /* SUM */
