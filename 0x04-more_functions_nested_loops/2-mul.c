#include <stdio.h>
#include "main.h"

/**
 * mul - multiple
 *
 * This function multiplies two integers
 * @one: first variable
 * @two: second variable
 *
 * Return: return 0 when done
 */
int mul(int one, int two)
{
	int res;

	res = one * two;
	return (res);
}
