#include <stdio.h>
#include "main.h"

/**
 * _islower - lowercase
 *
 * this function checks for lowercase character
 *
 * Return: 1 when true and 0 when none
 */
int _islower(int c)
{
	int obj;
	int c;

	obj = _islower(c);
	if (obj == 1)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
