#include <stdio.h>
#include "main.h"

/**
 * _islower - lowercase
 *
 * this function checks for lowercase character
 *
 * Return: 1 when true and 0 when none
 */
int main(void)
{
	int obj;
	int test;

	test = _islower(obj);
	if (test = 1)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
