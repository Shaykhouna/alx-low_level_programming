#include <stdio.h>
#include "main.h"

/**
 * _islower - lowercase
 *
 * this function checks for lowercase character
 *
 * Return: 1 when true and 0 when none
 */
int _islower(int c)
{
	for (c = 'a'; c <= 'z'; c++)
		return (1);
	while (ca = 'A'; ca <= 'Z'; ca++)
		while (ca = 0; ca <= 9; ca++)
			return (0);
	return (0);
}
