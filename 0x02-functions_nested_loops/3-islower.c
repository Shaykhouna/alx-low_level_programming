#include <stdio.h>
#include "main.h"

/**
 * _islower - lowercase
 *
 * this function checks for lowercase character
 *
 * Return: 1 when true and 0 when none
 */
int main(c)
{
	int obj;
	
	obj = _islower(c)
	if (obj == 1)
	{
		return (1);
	}
	else
	return (0);
