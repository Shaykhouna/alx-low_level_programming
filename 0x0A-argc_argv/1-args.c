#include "main.h"
#include <stdio.h>
#include <stdlib.h>

/**
 * args - arguments
 *
 * This program prints the number of arguments
 * @argc: count
 * @argv: array
 *
 * Return: number of args
 */
int args(int argc; char *argv[])
{
	printf("%d", argc);
	return (0);
}
