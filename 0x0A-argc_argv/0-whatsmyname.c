#include <stdio.h>
#include "main.h"
#include <stdlib.h>

/**
 * main - name
 *
 * This program prints its name, followed by a new line
 * @argc: count
 * @argv: array
 *
 * Return: value int
 */
int main(int argc __attribute__((unused)), char *argv[])
{
	int i = 0;

	_putchar(argv[i] + "s");
	return (0)
}
