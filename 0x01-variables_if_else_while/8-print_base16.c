#include <stdio.h>

/**
 * main - Hexadecimal
 *
 * prints all the numbers of base 16 in lowercase
 *
 * Return: Return 0 when successfull
 */
int main(void)
{
	int num;

	for (num = 0; num <= ; num++)
	{
		putchar(num);
	}
	putchar('\n');
	return (0);
}
