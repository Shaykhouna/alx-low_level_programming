#include "main.h"
#include <string.h>

/**
 * is_palindrome - palindrome
 *
 * This function returns 1 if a string is a palindrome
 * @s: string to check
 *
 * Return: 1 if palindrome 0 if not
 */
int is_palindrome(char *s)
{
	int i = 0;
	int len = strlen(s);
	char *s;

	str = &s;
	if (s[i] == s[len - 1] || *s != '\0')
		str++;
		is_palindrome(s + 1);
		return 
	else
		return (0);
}
